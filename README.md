# ts-express-api

Rest API

- Node
- Express
  - passport -> jwt
  - Joi
- TypeScript
- TypeORM
  - MariaDB
- Docker
- Jest

```
npm install
cp .env.example .env
docker-compose -f "docker-compose.dev.yml" up -d --build 
```
